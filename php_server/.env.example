# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cemilankasirpos_php_v02
DB_USER=root
DB_PASS=

# API Configuration
API_BASE_URL=http://localhost/php_server
SHOW_DEBUG_ERRORS=true

# Security Configuration
# Generate a strong random string for production!
JWT_SECRET=edit_your_jwt_secret_key_here_for_cemilankasirpos
# Comma separated list of allowed frontend origins (must match exactly: protocol + domain + port)
# Example: http://localhost:5173,https://toko-saya.com
# Required for cross-origin when frontend uses credentials: 'include'
ALLOWED_ORIGINS=http://localhost:5173
